#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import Executor
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  i32 Helloworld()'
  print '  bool StartTask(string task_ad)'
  print '  bool StopTask(i32 job_id, i32 task_id)'
  print '  bool KillTask(i32 job_id, i32 task_id)'
  print '  bool SendVMHeartbeat(VM_HbVMInfo hb_vm_info)'
  print '  bool AppInstalled(i32 job_id, i32 task_id, i32 app_id)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = Executor.Client(protocol)
transport.open()

if cmd == 'Helloworld':
  if len(args) != 0:
    print 'Helloworld requires 0 args'
    sys.exit(1)
  pp.pprint(client.Helloworld())

elif cmd == 'StartTask':
  if len(args) != 1:
    print 'StartTask requires 1 args'
    sys.exit(1)
  pp.pprint(client.StartTask(args[0],))

elif cmd == 'StopTask':
  if len(args) != 2:
    print 'StopTask requires 2 args'
    sys.exit(1)
  pp.pprint(client.StopTask(eval(args[0]),eval(args[1]),))

elif cmd == 'KillTask':
  if len(args) != 2:
    print 'KillTask requires 2 args'
    sys.exit(1)
  pp.pprint(client.KillTask(eval(args[0]),eval(args[1]),))

elif cmd == 'SendVMHeartbeat':
  if len(args) != 1:
    print 'SendVMHeartbeat requires 1 args'
    sys.exit(1)
  pp.pprint(client.SendVMHeartbeat(eval(args[0]),))

elif cmd == 'AppInstalled':
  if len(args) != 3:
    print 'AppInstalled requires 3 args'
    sys.exit(1)
  pp.pprint(client.AppInstalled(eval(args[0]),eval(args[1]),eval(args[2]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
